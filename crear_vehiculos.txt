from app.models import Repartidores, Vehiculos, Tipos_de_vehiculos, Estatus_de_vehiculos

# Eliminar vehículos antes de crear nuevos
Vehiculos.objects.all().delete()

# Obtener repartidores por nombre (más seguro que por ID)
repartidor1 = Repartidores.objects.get(nombre="Luis Martínez")
repartidor2 = Repartidores.objects.get(nombre="Juan Martínez")
repartidor3 = Repartidores.objects.get(nombre="Pablo Martínez")

# Crear vehículo para repartidor 1
Vehiculos.objects.create(
    tipo_de_vehiculo=Tipos_de_vehiculos.MOTO,
    modelo='Yamaha YBR 125',
    km_por_litro=35,
    id_repartidor=repartidor1
)

# Crear vehículo para repartidor 2
Vehiculos.objects.create(
    tipo_de_vehiculo=Tipos_de_vehiculos.CARRO,
    modelo='Aveo',
    km_por_litro=35,
    id_repartidor=repartidor2
)

# Crear vehículo para repartidor 3
Vehiculos.objects.create(
    tipo_de_vehiculo=Tipos_de_vehiculos.CARRO,
    modelo='Tiida',
    km_por_litro=35,
    id_repartidor=repartidor3
)
